/**
 * codex-threads-mcp
 *
 * Peer discovery and auto-reply messaging for local Codex threads.
 *
 * This package has two entry points:
 *   - server.ts  — MCP server (spawned by Codex or Claude Code, one per instance)
 *   - broker.ts  — Shared broker daemon (auto-launched, one per machine)
 *   - codex-bridge.ts — Codex thread wakeup bridge
 *
 * See README.md for setup and usage.
 */

export { } // module marker
